export const LOD_THRESHOLDS = {
  macro: 0.2,
  tactical: 0.8,
};

export const SERVER_TICK_MS = 10_000;
export const DEFAULT_MOVE_SPEED = 1; // world units per second
export const SIEGE_MINUTES = 60;

export const QUEUE_NAMES = {
  movement: "movement",
};
